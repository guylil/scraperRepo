{
  "name": "scraper",
  "version": "1.0.0",
  "description": "get data from sqs scrape instagram for the public data send the data to s3 as json",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.541.0",
    "puppeteer": "^1.20.0"
  }
}
